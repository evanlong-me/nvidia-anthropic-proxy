{
  "name": "nvidia-anthropic-proxy",
  "version": "1.0.0",
  "description": "Anthropic API compatible proxy for Nvidia API",
  "main": "index.js",
  "scripts": {
    "setup": "./setup.sh",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
